import React from 'react';
import RoutesConfig from './routes';
import '@shared/styles/global.css';

const App: React.FC = () => {
  return (
    <div>
      <RoutesConfig />
    </div>
  );
};

export default App;
